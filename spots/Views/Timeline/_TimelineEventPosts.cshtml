@model IEnumerable<spots.Modules.Timeline.Interfaces.ITimelineEvent>

<div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1 col-mg-8 col-md-offset-2 col-lg-6 col-lg-offset-3">
        @foreach (var post in Model)
        {
            @Html.Partial("_TimelineEventPost", post)
        }
    </div>
</div>